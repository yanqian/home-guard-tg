{
  "features": [
    {
      "id": "F001",
      "description": "Create a standalone trusted-host Telegram polling Bot for personal home-watch use with /camera_clip <seconds>, /status, and /help; validate TELEGRAM_BOT_TOKEN and ALLOWED_CHAT_IDS, persist runtime_state.json with Telegram update offsets, restrict commands to authorized chats, parse ENABLE_CAMERA_CLIP_COMMAND and CAMERA_CLIP_COMMAND_JSON as a shell-disabled argv array template with {seconds} and {output}, record clips for integer durations from 1 through 10, reject disabled or malformed capture config, reject concurrent capture, enforce capture timeout, send successful clips through Telegram sendVideo, delete temporary media after success or failure, avoid Codex and repository workflow commands, document setup and BotFather commands, and add unit, harness, contract, smoke, and init coverage using fake capture and fake Telegram transport without real camera or network requirements.",
      "passes": true,
      "status": "done",
      "attempts": 1,
      "last_error": "",
      "priority": "P0"
    }
  ]
}
